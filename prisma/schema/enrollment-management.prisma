// model Enrollment {
//   id             String    @id @default(auto()) @map("_id") @db.ObjectId
//   studentId      String    @db.ObjectId
//   sectionId      String    @db.ObjectId
//   semesterId     String    @db.ObjectId
//   enrollmentDate DateTime  @default(now())
//   status         String
//   grade          String?
//   remarks        String?
//   dateCreated    DateTime  @default(now())
//   dateUpdated    DateTime  @updatedAt

//   // Relations
//   student        StudentManagement   @relation(fields: [studentId], references: [id])
//   section        SectionManagement   @relation(fields: [sectionId], references: [id])
//   semester       SemesterManagement  @relation(fields: [semesterId], references: [id])
// }


model StudentEnrollment {
  id              String      @id @default(auto()) @map("_id") @db.ObjectId
  schoolId       String                  @unique                     // school id
  firstName      String
  lastName       String
  middleName     String?  
  courseCode      String      @unique
  courseName      String
  description     String?
  units           Int
  department      String
  prerequisites   String[]

  // Section-specific fields
  maxCapacity     Int?
  day             String?
  timeStart       String?     // e.g. "08:00 AM"
  timeEnd         String?     // e.g. "10:00 AM"
  room            String?
  instructor      String?
  semester        String?
  yearLevel       String?
  status          String?

  // Timestamps
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
}
