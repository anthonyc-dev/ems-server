<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>🎓 EMS Backend Server</title>
    <style>
      /* Global Reset */
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family: "Inter", "Segoe UI", "Roboto", sans-serif;
        background: radial-gradient(
          circle at 20% 30%,
          #0f172a 0%,
          #020617 100%
        );
        color: #f9fafb;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        overflow: hidden;
      }

      /* Main Container */
      .container {
        background: rgba(255, 255, 255, 0.07);
        backdrop-filter: blur(18px);
        border: 1px solid rgba(255, 255, 255, 0.12);
        border-radius: 20px;
        padding: 3rem 2.5rem;
        max-width: 520px;
        text-align: center;
        box-shadow: 0 0 25px rgba(0, 0, 0, 0.4);
        transition: all 0.3s ease-in-out;
      }

      .container:hover {
        transform: translateY(-5px);
        box-shadow: 0 0 35px rgba(37, 99, 235, 0.3);
      }

      /* Logo Animation */
      .docker-logo {
        height: 70px;
        margin-bottom: 1.5rem;
        animation: float 3s ease-in-out infinite;
        filter: drop-shadow(0 0 12px rgba(14, 165, 233, 0.3));
      }

      @keyframes float {
        0%,
        100% {
          transform: translateY(0);
        }
        50% {
          transform: translateY(-10px);
        }
      }

      /* Title & Subtitle */
      .title {
        font-size: 2.2rem;
        font-weight: 800;
        color: #3b82f6;
        margin-bottom: 0.5rem;
        letter-spacing: 1px;
      }

      .subtitle {
        font-size: 1.1rem;
        color: #e2e8f0;
        margin-bottom: 2rem;
      }

      /* Academic Divider */
      .divider {
        width: 60px;
        height: 4px;
        background: linear-gradient(90deg, #3b82f6, #06b6d4);
        border-radius: 2px;
        margin: 1rem auto 1.8rem;
      }

      /* Tech Stack Badges */
      .badges {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 0.6rem;
      }

      .badge {
        background: rgba(59, 130, 246, 0.15);
        border: 1px solid rgba(59, 130, 246, 0.4);
        color: #93c5fd;
        padding: 0.45em 1em;
        border-radius: 8px;
        font-family: "Fira Code", monospace;
        font-weight: 600;
        font-size: 0.9rem;
        transition: all 0.2s ease;
      }

      .badge:hover {
        background: rgba(59, 130, 246, 0.25);
        transform: scale(1.05);
      }

      /* Footer */
      .footer {
        margin-top: 2.3rem;
        font-size: 0.9rem;
        color: #9ca3af;
        letter-spacing: 0.03em;
        line-height: 1.5;
      }

      .footer span {
        color: #a5f3fc;
      }

      /* Responsive Design */
      @media (max-width: 480px) {
        .container {
          padding: 2.2rem 1.5rem;
        }
        .title {
          font-size: 1.8rem;
        }
        .subtitle {
          font-size: 1rem;
        }
      }
    </style>
  </head>

  <body>
    <div class="container">
      <img
        class="docker-logo"
        src="https://www.docker.com/wp-content/uploads/2022/03/Moby-logo.png"
        alt="Docker Logo"
      />
      <div class="title">Enrollment Management System</div>
      <div class="subtitle">
        Backend Server is <strong>Dockerized</strong> and ready for scalable
        deployment.
      </div>
      <div class="divider"></div>

      <div class="badges">
        <span class="badge">TypeScript</span>
        <span class="badge">Express.js</span>
        <span class="badge">Prisma ORM</span>
        <span class="badge">MongoDB</span>
        <span class="badge">REST API</span>
        <span class="badge">Docker</span>
      </div>

      <div class="footer">
        🎓 <span>EMS Backend</span> — Secure, containerized, and optimized for
        academic operations.<br />
        Powered by <span>Node.js</span> • Designed for <span>Scalability</span>.
      </div>
    </div>
  </body>
</html>
